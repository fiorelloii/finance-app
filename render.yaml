services:
  - type: web
    name: finance-app-backend
    env: node
    plan: free
    buildCommand: npm install --prefix apps/backend && npm run build --prefix apps/backend
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
